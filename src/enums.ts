export enum CubePlane {
  X = 'x',
  Y = 'y',
  Z = 'z'
}

export enum Side {
  L = 'L',
  R = 'R',
  U = 'U',
  D = 'D',
  F = 'F',
  B = 'B'
}

export enum Modifier {
  Double = '2',
  CounterClockwise = '\'',
  Clockwise = ''
}